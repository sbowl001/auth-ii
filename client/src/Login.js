import React, { Component } from 'react';
import {Link} from 'react-router-dom';

const Login = props => {
    const linkStyle = {
        margin: 10
    }
return (
    <div> 
    Login
    </div>
)
}

export default Login;